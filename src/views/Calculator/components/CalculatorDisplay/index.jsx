import { StyledCalculatorDisplay } from '../../styles/styles.jsx';

export default function CalculatorDisplay({ calcValue }) {

    return(
        <StyledCalculatorDisplay>{calcValue}</StyledCalculatorDisplay>
    )
}